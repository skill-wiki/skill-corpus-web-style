<atom
  id="@web-style/rule-one-binding-per-declaration"
  kind="rule"
  version="0.1.0"
  domain="web-style"
  tokens="180"
  q="4.0">
  <description>Write `const a = 1; const b = 2;` rather than `const a = 1, b = 2;`. One declaration per binding makes diffs cleaner, lets you initialise each from a different expression without contortions, and avoids the trap of accidentally giving every name in a comma-separated list the same `const`-vs-`let` modifier.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="92"/>
    <level name="core"    file="chunks/core.md"    tokens="180"/>
    <level name="full"    file="chunks/full.md"    tokens="195"/>
  </projection>
</atom>
