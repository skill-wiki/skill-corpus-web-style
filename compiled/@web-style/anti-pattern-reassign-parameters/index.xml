<atom
  id="@web-style/anti-pattern-reassign-parameters"
  kind="anti-pattern"
  version="0.1.0"
  domain="web-style"
  tokens="313"
  q="4.0">
  <description>Treating a parameter as a mutable local — assigning to it, mutating its properties when the caller did not opt in, or using it as scratch space. The parameter binding is part of the function&apos;s contract with its callers; rewriting it in the body breaks the obvious mapping between the call site and what runs.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="93"/>
    <level name="core"    file="chunks/core.md"    tokens="313"/>
    <level name="full"    file="chunks/full.md"    tokens="534"/>
  </projection>
</atom>
