<atom
  id="@web-style/rule-group-const-then-let"
  kind="rule"
  version="0.1.0"
  domain="web-style"
  tokens="165"
  q="4.0">
  <description>Within a scope, list every `const` binding before any `let` binding. The reader can then scan the top of the scope and immediately see which bindings are fixed and which will mutate, without re-checking each line.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="67"/>
    <level name="core"    file="chunks/core.md"    tokens="165"/>
    <level name="full"    file="chunks/full.md"    tokens="186"/>
  </projection>
</atom>
